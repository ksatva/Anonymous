#!/bin/bash
# SETTING UP GIT FOR FIRST TIME IN A SYSTEM

git config --global user.name "scholark"
git config --global user.email "ksrt@outlook.com"
#git config --global color.ui auto

git init

git status
git add -A
git commit -m "First commit to this repository"

#git push


